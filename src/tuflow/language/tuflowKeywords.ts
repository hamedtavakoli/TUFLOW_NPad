import { getTuflowCommandTokenWords } from '../../lib/commands';

const nppTuflowKeywordXml = `
<KeywordLists>
<Keywords name="Keywords1">==</Keywords>
<Keywords name="Keywords2">(FT) (H) (M) (N,M) (S) (X,Y) [DEPTHS | VXD] [WU] 1D 2D 12DA 1D2D 2D2D A ABOVE ACCESS AD ADD ADDITIONAL ADJUST ADJUSTMENT AEP AFTER AIR ALIGNMENT ALL ALLOW AND ANGLE ANGLES APPLY APPROACH AREA ARI AS ASC AT AUTO AUTOMATIC BALANCE BASE BC BED BEND/DROP BG BLANK BLOCKAGE BLUE BNDY BOUNDARIES BOUNDARY BOUNDS BREACH BREACHED BREAKLINE BRIDGE BUILD BSS C CALCULATION CALIBRATION CAPACITY CCA CD CELL CELLS CENTRED CF CH CHANGE CHANNEL CHANNELS CHECK CHECKS CHEZY CL CLEARANCE CLOSED CNM CNM CODE COEFFICIENT COEFFICIENTS COINCIDENT COLLAPSE COMMAND COMMANDS COMPRESSION CONNECTION CONNECTIONS CONTINUING CONTROL CONTROLS CONVEYANCE CORES CORNERS COUNT CREATE CREEP CRITICAL CROSS-SECTIONS CSV CULVERT CUT CUTOFF CV CYCLONE CORRECTOR CORNER CPU CROSSFALL COURANT CURVE CWF DAM DANGLING DAT DATA DATABASE DATE DEBRIS DEFAULT DEFAULTS DEFINE DELTA DEMO DENSITY DEPTH DEPTH/RIPPLE DEPTHS DETECTION DIMENSION DIRECTION DISCHARGE DISPLAY DISTANCE DISTRIBUTE DISTRIBUTION DOES DOMAIN DONGLES DOUBLE DP DRIVE DRYING DURING DYNAMIC DEVICE DV EB ELSE EMPTY END ENGELHUND ENTIRE EQUATION ESTRY EVENT EVENTS EX EXCEL EXIT EXPONENT EXT EXTERNAL EXTRAPOLATE EXTENT EXCLUDE FACTOR FAILURE FC FILE FILENAME FILES FIND FIRM FIRST FLAG FLC FLC/L FLOW FLOWS FLT FOLDER FOR FORCE FORMAT FORMULA FORMULATION FORWARD FRAC FREE FREQUENCY FRIC FROUDE FULL FULLY GA GATE GAUGE GAUGES GENERAL GEOMETRY GIS GLO GLOBAL GPU GRID GULLY GRID GRIDDED GRIDS H/D HARDWARE HAZARD HCN HEAD HEADS HEIGHT HPC HQ HIGH HR HRASC HRFLT HURRICANE HX HYETOGRAPH IDS IDW IF IGNORE IGNORED IL IN INCLUDE INDEX INFILTRATION INITIAL INLET INPUT INSIDE INSTABILITY INTACT INTERFACE INTERPOLATE INTERPOLATION INTERVAL INVERTS ISIS ITERATE ITERATIONS IWL JUNCTIONS K KA KP KENUE LAND LATITUDE LAYERED LENGTH LEVEL LIMIT LINE LINES LINK LINKED LINKS LOCATION LOCATIONS LOG LOSS LOSSES LP M M11 MANHOLE MANHOLES MANNING MANNINGS MAP MASS MAT MAX MATERIAL MATERIALS MAXIMUM MAXIMUMS MAX/MIN MESH MESHPARTS METHOD MI MID MIN MINIMUM MINIMUMS MODEL MOISTURE MOMENTUM MULTIPLIER MATRIX MIN(ZU,ZV) N NA NAR NAME NC NEGATIVE NESTING NETCDF NETCDF NETWORK NO NODE NODES NONE NOT NULL NUMBER NAN NAN OBJECT OBJECTS OBLIQUE OBVERT OF OFF OFFSET ONE ONLINE ONLY OPEN OPENING OPENING/CLOSING OPERATING OPERATION OPERATIONAL OPTIMISATION ORDER ORIFICE ORIENTATION ORIGIN OUTPUT OVERFALL PARTIAL PATH PAUSE PEER PERIOD PIPE PIT PITS PLATFORM PLOT PO POINT POINTS POLY POLYLINE POLYGON POLYGONS POROSITY PRECISION PROCESS PROCESSING PROFILES PROJECT PROJECTION PROPORTION PUMP Q QUADTREE R RADIATION RADIUS RAINFALL RAM RANGE RATE RATIO READ RECALCULATE RECEPTOR RECEPTORS RELEASE REGION RELATIVE REPEATED REPEATS REPORTING RES RESISTANCE RESTART RETRY RETURN REVEAL RF RIDGE ROAD ROUTE ROWCOL S SA SAME SAMPLE SAVE SB SCENARIO SCENARIOS SCHEME SCREEN/LOG SEARCH SEAT SELECTION SERIES SET SETTINGS SF SGS SHALLOW SHAPE SHP SIDE SIDES SIMULATIONS SIZE SLASH SLOPE SMOOTHED SMS SNAP SOIL SOILS SOLVER SOURCE SPEED SPILLWAY START STARTUP/SHUTDOWN SLUICE STEPPED STOP STORAGE STORE STRESSES STRING STRUCTURE SUBMERGED SUPERCRITICAL SUPPORTED SURCHARGE SWEEP SX SYNCHRONISATION SOLUTION SRF STREAMS STRESS T TABLE TAPER TAU TEMPORAL TERMINATE TEST TEXT TGF TGO THICK THIN THREADS TIME TIMES TIMESTEP TIMESTEPS TIN TLF TMO TO TOLERANCE TOP TRACK TREATMENT TRIANGLES TRIGGER TRIM TRIANGLE TSF TUFLOW TUTORIAL TYPE TYPES UK UNIT UNITS UNUSED UNSUPPORTED UPDATE USE VALUE VALUES VARIABLE VECTOR VEL VELOCITY VERBOSE VERSION VERTICAL VERTICES VG VISCOSITY WARMUP WARNING WATER WAVE WDAY WEIGHTING WEIR WEIRS WET WET/DRY WETTING WHEN WIBU WIDTH WIND WIND/WAVE WLL WLLP WLLP WR WRB WRC WRF WRF WRR WRITE WU X1D XF XMDF XP XS XZ Z ZC ZERO ZH ZHC ZONE ZONES ZP ZPT ZPTS ZUV ZUVC ZUVH AIR ALPHAR APPROACH AREA ARMOUR AUTO AUTOMATIC BC BC BED BOTTOM BOUNDARIES BOUNDARY C CELL CFL CHANGE CHECK CHECKS COEFFICIENT CYC CYC_HOLLAND COORDINATE CONCENTRATION COUPLING CONDITION CONDITIONS COMMANDS CONNECTION CONNECTIONS CONTROL CORIOLIS CREATE CREEP CRITICAL CROSS-SECTIONS CSV CULVERT CUT CUTOFF CV CYCLONE D50 DATA DATABASE DATE DATV DEFAULT DEFAULTS DEFINE DENSITY DEPTH DEPTHS DEPOSITION DIFFUSIVITY DIR DISPERSION DISPLAY DISTANCE DISTRIBUTION DOUBLE DRAG DRIFT DRYING DT DYNAMIC DRY ECHO EDDY ELEVATION END BC END OUTPUT EQUATION EULERIAN EXTERNAL EROSION FACES FACTOR FB FC FILE FLOW FLOWS FOLDER FORMAT FORMULA FORMULATION FRACTION G GAUGES GENERAL GEOMETRY GIS GLOBAL GRADIENT GROUP GROUPS HEAD HEADER HEAT HEIGHT HIGH HORIZONTAL HOURS HUV IC IN INCLUDE INCLUDES INITIAL INPUT INSTABILITY INTERNAL INTERVAL ISODATE JUNCTIONS KINEMATIC KS LAGRANGIAN LAND LATITUDE LAYERS LENGTH LEVEL LIMIT LIMITS LIMITER LOCATION LOG LAYER LOAD LOGDIR MANNING MASS MAT MATERIAL MAXIMUM MESH METHOD MIN MIXING MODEL MODESPLIT MOMENTUM MORFAC MORPHOLOGICAL MSLP MSLP_GRID NODESTRING NSCALAR NTRACER NUMBER NLAYER OF OFFSET OP OPEN ORDER OUTPUT OVERWRITE PARAMS PARAMETERS PARTICLE PARTICLES PERIOD POINT POINTS POLYGON PROFILE PROJECTION Q QC QCA QG QGA QUALITY R RADIATION RATE READ REFERENCE RESET RESTART RNS RS ROUGHNESS SALINITY SEA SAVE SCALE SCALAR SCREEN/LOG SEARCH SEDIMENT SEED SET SETTLING SHEAR SHP SIDE SIDES SIGMA SIZE SLUMPING SMS SOURCE SPATIAL SPHERICAL STABILITY START STATE STOP STRESS STRESSES STRUCTURE SUB-TYPE SUFFIX SURFACE SUSPENDED TEMPERATURE THICKNESS TIME TIMES TIMESERIES TIMESTEP TRACKING TUFLOW TURBULENCE TUTORIAL TYPE TYPES USE UPDATE VALUES VELOCITY VERTICAL VISCOSITY W10 W10_GRID WARMUP WATER WAVE WEIR WEIRS WET/DRY WETTING WIDTH WIND WIND/WAVE WL WLS WQ WRITE X1D XF XMDF XP XS XZ Z ZERO</Keywords>
</KeywordLists>
`;

export interface NotepadPlusKeywordGroups {
  operators: string[];
  keywords: string[];
}

const nppKeywordGroups = parseNotepadPlusKeywordXml(nppTuflowKeywordXml);
const catalogKeywordWords = getTuflowCommandTokenWords();

export const tuflowLanguageRules = {
  caseInsensitive: true,
  lineCommentPrefixes: ['!', '#', '//'],
  operators: nppKeywordGroups.operators,
  keywords: uniqueWords([...nppKeywordGroups.keywords, ...catalogKeywordWords].join(' ')),
  commandLeadWords: [
    'BC',
    'Cell',
    'Control',
    'Define',
    'Event',
    'Geometry',
    'Output',
    'Read',
    'Set',
    'Write'
  ],
  includeReadWords: ['Include', 'Read', 'Geometry', 'BC', 'Event']
} as const;

export const tuflowKeywordSet = new Set(tuflowLanguageRules.keywords.map((word) => word.toLowerCase()));
export const tuflowCommandLeadWordSet = new Set(tuflowLanguageRules.commandLeadWords.map((word) => word.toLowerCase()));
export const tuflowIncludeReadWordSet = new Set(tuflowLanguageRules.includeReadWords.map((word) => word.toLowerCase()));

export function isTuflowKeyword(value: string): boolean {
  return tuflowKeywordSet.has(value.toLowerCase());
}

export function parseNotepadPlusKeywordXml(xml: string): NotepadPlusKeywordGroups {
  const groups = Array.from(xml.matchAll(/<Keywords name="([^"]+)">([\s\S]*?)<\/Keywords>/g));
  const keywords = groups
    .filter((match) => match[1] === 'Keywords2')
    .flatMap((match) => splitWords(match[2]));
  const operators = groups
    .filter((match) => match[1] === 'Keywords1')
    .flatMap((match) => splitWords(match[2]));

  return {
    operators: uniqueWords(operators.join(' ')),
    keywords: uniqueWords(keywords.join(' '))
  };
}

function uniqueWords(text: string): string[] {
  return Array.from(new Set(splitWords(text))).sort((a, b) => a.localeCompare(b));
}

function splitWords(text: string): string[] {
  return text.split(/\s+/).map((word) => word.trim()).filter(Boolean);
}
